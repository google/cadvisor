run:
  timeout: 5m
  linters-settings:
    govet:
      enable-all: true
    gofmt:
      simplify: true
    goimports:
      local-prefixes: github.com/google/cadvisor
linters:
  disable-all: true
  enable:
    - govet
    - errcheck
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - typecheck
    - gofmt
    - goimports
issues:
  max-issues-per-linter: 0
  max-same-issues: 0
